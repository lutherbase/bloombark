node_modules/

# backend/.env IS tracked (shared default config, no real secrets — see the file
# header). Local machine-specific overrides can go in backend/.env.local instead.
.env.local
backend/.env.local
!.env.example
!backend/.env.example
backend/.secrets.json

# Backend now runs on MySQL — this leftover SQLite file is no longer read by
# the app. Kept on disk only in case old data needs to be migrated manually.
backend/bloombark.db
